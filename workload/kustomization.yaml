apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied INTO the workload cluster (the Sylva unit keeps the default
# kubeConfig.secretRef from workload-cluster.values.yaml). Namespace comes from the
# unit's targetNamespace: sandbox.

resources:
- sctp-module-loader.yaml
